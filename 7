bool edacl ()
    {

        if (cx[n]==foodx && cy[n]==foody)// и
        {
            foodx=NULL;
            foody=NULL;
            score++;
            return true;
        }

        return false;
    }
